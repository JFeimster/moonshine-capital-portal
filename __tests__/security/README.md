# Security tests

Focused regression coverage for authorization boundaries, redirect safety, webhook rejection, and outage fallback expectations.

Application-specific unit tests remain in their existing suites unless moving them materially improves clarity.
